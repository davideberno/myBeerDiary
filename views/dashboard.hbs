<h1 class="mt-4 ml-4 text-white">Welcome {{user}}</h1>
<div class="container">
  <div class="row">
    {{#if comments}}
    {{#each comments}}
    {{> beerCardProfile}}
    {{/each}}
    {{/if}}
  </div>
</div>